# UMG Envoy Agent v0.2.8 Release Checklist

UMG Envoy Agent v0.2.8 is a metadata-only install validation correction release. It changes `openclaw.install.minHostVersion` from `>=2026.3.23-1` to `>=2026.3.23` to satisfy OpenClaw installer validation. No runtime, compiler bridge, artifact resolver, relation matrix, sleeve, block, NeoBlock, or NeoStack behavior changes are included.
