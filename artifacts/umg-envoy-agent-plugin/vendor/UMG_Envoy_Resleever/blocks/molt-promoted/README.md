# Promoted MOLT Blocks

Conservative top-level promoted MOLT store derived from extracted sleeve artifacts.

These files contain sleeve-derived blocks that were not automatically merged into the original normalized `blocks/molt/*` libraries during Phase 5.
