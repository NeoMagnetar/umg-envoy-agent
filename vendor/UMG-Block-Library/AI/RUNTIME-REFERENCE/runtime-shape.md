# runtime-shape

Public runtime reference is intentionally minimal in the first pass.

Private runtime-active state, traces, compile outputs, backups, and staging history are excluded from this public repo.
When public-safe sample runtime documents are curated, they can be added here.
